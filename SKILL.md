---
name: video-narration-subtitles
description: 为无声或原声不适合使用的录屏视频制作简洁中文解说、微软 Edge TTS 配音和可读字幕成片；适用于产品演示、Skill 能力演示和客户端操作录屏。
---

# 视频解说与字幕

用于把录屏整理成清楚说明“用户做了什么 → 产品或系统如何响应 → 执行了什么能力或流程 → 输出了什么”的演示，不逐字复述屏幕上的编号、价格、字段或其他界面细节。适用于任意平台、产品或客户端，不预设产品名称、厂商、功能类型或技术实现。

## 任务启动

先结合用户说明、视频元信息和关键帧识别产品/平台、演示流程、界面术语和可讲述的能力链路。录屏信息足够时直接进入文案阶段；不要因为平台不是已知产品而中断。

若无法可靠判断叙事重点、术语含义、受众或原声音频的处理方式，在产出文案前向用户收集必要信息。优先提出简短、与当前视频相关的问题，可包括：

- 产品或平台名称、演示功能和目标受众。
- 希望观众理解的核心价值或行动结果。
- 应保留的品牌/产品术语、正确读法和禁止提及的内容。
- 原声是否保留，以及录屏中需要避让或脱敏的区域。

不要根据界面风格、图标或个别文案武断推断产品归属；不确定时以中性表述写出待确认的文案。

## 固定偏好

- 优先使用微软 Edge TTS：`edge-tts --voice zh-CN-XiaoxiaoNeural`。
- 默认语速为 `--rate='+10%'`。
- 配音文本与字幕文本分开维护：字幕可以保留产品术语 `Skill`，配音用“技能”；避免让 TTS 直接朗读英文术语。
- 避免多音字误读：若 TTS 将“差”在业务复合词中读错，优先改写为“出差”；例如“差标”改为“出差标准”，“差旅行程”改为“出差行程”，“差旅合规筛查”必要时改为“出差合规筛查”。
- 字幕为纯黑不透明圆角矩形、纯白字；字号小而清晰，文字在背景框内上下居中，整体位于画面底部但避开输入框/操作栏。
- 原视频无有效音轨时直接替换为配音；若有需要保留的原声，应先与用户确认混音比例。

## 工作流

1. 检查视频时长、分辨率和音轨；浏览关键帧，识别录屏流程和产品语境；必要时补齐任务启动信息。
2. 先产出简洁的 5–8 段解说文案，让用户确认后再生成音视频。
3. 为每段维护开始时间、结束时间、字幕文本和可选的 `tts_text`。字幕时间轴应覆盖整段演示，配音不应超出下一段开始时间。
4. 用 Edge TTS 逐段生成音频，用 `ffmpeg` 按开始时间延迟并混合为完整单声道 AAC 音轨。
5. 用 Pillow 生成与视频同尺寸的 RGBA 字幕图层：黑色圆角框、白字、按可用宽度自动换行，使用 PingFang 字体并用 `multiline_textbbox` 校正垂直基线。
6. 将字幕图层按时间段 concat 后一次性 overlay 到原视频，输出 H.264/AAC MP4；同时输出独立 SRT 和配音音频。
7. 至少抽查开头、中段、结尾画面，确认字幕不遮挡关键操作、字体上下居中、术语显示正确，并用 `ffprobe` 检查音视频流和时长。

## 推荐的文案视角

只描述行为和能力链路，例如：用户提交需求；产品识别目标并启动对应功能或自动化流程；系统查询所需信息、结合约束生成方案；用户确认后继续完成下一步操作。根据视频实际内容选择“产品”“系统”“工作流”“智能体”等准确称呼，不要把屏幕上具体结果逐项念出来。

## 依赖

流程需要 `ffmpeg`、`edge-tts`、Pillow 和系统 PingFang 字体；不使用 Homebrew 安装依赖。可在每次任务中按实际时间轴生成临时 TTS 音频和字幕图层，再用 ffmpeg 合成。
